---
const { title, subtitle, backgroundImage } = Astro.props;
---

<section class="relative h-screen bg-cover bg-center flex items-center justify-center" style={`background-image: url(${backgroundImage})`}>
  <div class="text-center px-6">
    <h1 class="text-6xl font-bold text-white mb-4">{title}</h1>
    <p class="text-lg text-gray-200">{subtitle}</p>
    <a href="#next-section" class="mt-6 inline-block bg-blue-500 text-white py-3 px-6 rounded-full hover:bg-blue-600">
      Learn More
    </a>
  </div>
</section>
